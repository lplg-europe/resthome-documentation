# Les blocs « grid cards » et les encadrés « !!! note / warning » de Material
# for MkDocs reposent sur une mise en forme précise (indentation, listes HTML).
# Un formateur Markdown automatique (Prettier, markdownlint --fix) les APLATIT
# et casse silencieusement les pages. On exclut donc le contenu de la doc.
docs/
